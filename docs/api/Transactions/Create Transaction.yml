info:
  name: Create Transaction
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/transactions"
  headers:
    - name: Authorization
      value: Bearer {{ACCESS_TOKEN}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
